# Python
__pycache__/
*.pyc
.DS_Store

# Per-user Task Graphs and local environments
task_data/
.venv/
.uv-cache/

# Temporary graph build directories (the completed graph is published)
graph_rag_data/.build-staging-*/
graph_rag_data/.enhance-staging-*/
graph_rag_data/.compact-staging-*/

# Raw ERP export is a local build source (8+ GB); publish the ready graph instead
ERPcode/
manifest.json
manifest.ndjson

# API keys
.env
