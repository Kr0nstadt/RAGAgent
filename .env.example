# Required only for Pi/Herdr and commands that explicitly use an LLM.
# Graph search, validation, Task Graph creation and planning work without it.
WORMSOFT_API_KEY=

# Optional fallback provider.
DEEPSEEK_API_KEY=

# Leave disabled for graph-only commands.
RAG_ENABLE_LLM=0
